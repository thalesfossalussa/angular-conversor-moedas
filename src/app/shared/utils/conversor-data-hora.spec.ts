import { ConversorDataHora } from './conversor-data-hora';

describe('ConversorDataHora', () => {
  it('should create an instance', () => {
    expect(new ConversorDataHora()).toBeTruthy();
  });
});
