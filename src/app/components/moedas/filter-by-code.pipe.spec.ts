import { FilterByCodePipe } from '../filter-by-code.pipe';

describe('FilterByCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCodePipe();
    expect(pipe).toBeTruthy();
  });
});
